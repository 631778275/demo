var omit = (function(text, length) {
    if (!text) {
        return ('')
    };
    console.log(text.title);
    var short = text.title.substring(0, length);
    console.log(short);
    return (short + '...')
});
module.exports = ({
    omit: omit,
});